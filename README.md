# md-viewer

A simple local HTML page that lets you open an `.md` file and read it rendered in your web browser, using client-side JavaScript. No server, no build step — just open `index.html`.

## Status

Scaffolding. The viewer itself is not built yet.
